{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"clang-format-inc","text":"<p>Incremental C/C++ formatting as a pre-commit hook \u2014 format only the lines you changed.</p> <p> </p>"},{"location":"#the-problem","title":"The problem","text":"Tool Issue <code>mirrors-clang-format</code> Reformats entire files \u2014 pollutes diffs in large codebases <code>git clang-format --staged</code> Works locally, but staging area is empty in CI when pre-commit uses <code>--from-ref</code>/<code>--to-ref</code> <p><code>clang-format-inc</code> solves both. It reads the <code>PRE_COMMIT_FROM_REF</code> / <code>PRE_COMMIT_TO_REF</code> environment variables that pre-commit sets in CI mode and falls back to <code>git diff --cached</code> locally.</p> <p>Inspired by <code>darker</code>, which does the same thing for Python/Black.</p>"},{"location":"#quick-start","title":"Quick start","text":"<p>Add to your <code>.pre-commit-config.yaml</code>:</p> <pre><code>repos:\n  - repo: https://github.com/goyaladitya05/clang-format-inc\n    rev: v0.3.0\n    hooks:\n      - id: clang-format-inc\n</code></pre> <p><code>clang-format</code> is installed automatically \u2014 no system-wide install needed.</p> <p>Then:</p> <pre><code>pip install pre-commit\npre-commit install\n</code></pre> <p>That's it. Every <code>git commit</code> now formats only the lines you changed.</p>"},{"location":"#why-incremental","title":"Why incremental?","text":"<p>Imagine you're working on a large legacy codebase where half the files aren't yet formatted. Running <code>clang-format</code> on whole files every commit would:</p> <ul> <li>Reformat thousands of untouched lines</li> <li>Make every PR unreadable \u2014 formatting noise drowns real changes</li> <li>Trigger conflicts with teammates working on the same files</li> </ul> <p>Incremental formatting enforces style only on new code \u2014 the code you actually wrote. Legacy lines stay untouched until someone deliberately reformats them.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>The full changelog is maintained in the repository:</p>"},{"location":"changelog/#changelog_1","title":"Changelog","text":"<p>All notable changes to <code>clang-format-inc</code> are documented here.</p> <p>The format follows Keep a Changelog. This project adheres to Semantic Versioning.</p>"},{"location":"changelog/#unreleased","title":"Unreleased","text":""},{"location":"changelog/#030-2026-03-26","title":"0.3.0 \u2014 2026-03-26","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>--diff</code> mode: print a unified diff of what would change without modifying   files; exits non-zero if any changed line would be reformatted \u2014 useful for PR   comment bots and debugging. Mutually exclusive with <code>--check</code>.</li> <li><code>--include REGEX</code>: only process files whose path matches the given regular   expression; useful when running standalone (outside pre-commit).</li> <li><code>--exclude REGEX</code>: skip files whose path matches the given regular expression.</li> <li><code>--workers N</code>: process files in parallel using N clang-format processes   (default 1); useful for large CI jobs with many changed files.</li> <li>README: document untracked-files behaviour (new files are formatted in full   since there is no prior revision to diff against).</li> </ul>"},{"location":"changelog/#020-2026-03-26","title":"0.2.0 \u2014 2026-03-26","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li><code>--check</code> mode: run without modifying files; exits non-zero and prints a message   if any changed line would be reformatted \u2014 suitable for CI pipelines that want to   report formatting issues without auto-applying fixes</li> <li><code>additional_dependencies: ['clang-format']</code> in <code>.pre-commit-hooks.yaml</code>: pre-commit   now installs the <code>clang-format</code> PyPI package automatically into the hook virtualenv,   removing the requirement for a system-installed <code>clang-format</code>; users can pin a   specific version via <code>additional_dependencies: ['clang-format==18.1.0']</code> in their   own <code>.pre-commit-config.yaml</code></li> </ul>"},{"location":"changelog/#010-2026-03-26","title":"0.1.0 \u2014 2026-03-26","text":"<p>Initial release.</p>"},{"location":"changelog/#added_2","title":"Added","text":"<ul> <li>Incremental C/C++ formatting: format only lines changed in a diff, not whole files</li> <li>CI mode: reads <code>PRE_COMMIT_FROM_REF</code> / <code>PRE_COMMIT_TO_REF</code> environment variables   set by pre-commit when running with <code>--from-ref</code>/<code>--to-ref</code>; diffs those two refs</li> <li>Local mode: falls back to <code>git diff --cached</code> (staged changes) when env vars are absent</li> <li>Partial env var guard: if only one of the two CI env vars is set, falls back to local   mode rather than crashing</li> <li><code>--binary</code> option to specify a custom <code>clang-format</code> executable path</li> <li><code>--style</code> option (default: <code>file</code>, reads <code>.clang-format</code> from the repo)</li> <li><code>--fallback-style</code> option for repos without a <code>.clang-format</code> file</li> <li><code>-p</code> option to control path-prefix stripping in the diff (default: <code>1</code>)</li> <li>Early exit when no diff output \u2014 skips <code>clang-format-diff.py</code> entirely</li> <li>Friendly error message when <code>clang-format</code> binary is not found on <code>PATH</code></li> <li>Bundled <code>clang-format-diff.py</code> from LLVM (Apache-2.0 WITH LLVM-exception) \u2014 zero   runtime dependencies beyond Python and a <code>clang-format</code> binary</li> <li><code>.pre-commit-hooks.yaml</code> so this repo can be used directly as a pre-commit hook source</li> <li><code>py.typed</code> marker for PEP 561 compatibility</li> </ul>"},{"location":"cli/","title":"CLI Reference","text":"<pre><code>clang-format-inc [OPTIONS] [FILES ...]\n</code></pre> <p><code>FILES</code> are the C/C++ files to consider. When used as a pre-commit hook, pre-commit passes the staged files automatically.</p>"},{"location":"cli/#options","title":"Options","text":""},{"location":"cli/#-binary-path","title":"<code>--binary PATH</code>","text":"<p>Path to the <code>clang-format</code> executable.</p> <p>Default: <code>clang-format</code> (resolved from <code>PATH</code>)</p> <pre><code>clang-format-inc --binary /usr/lib/llvm-18/bin/clang-format\n</code></pre>"},{"location":"cli/#-style-style","title":"<code>--style STYLE</code>","text":"<p>Formatting style passed to <code>clang-format</code>.</p> <p>Default: <code>file</code> (reads <code>.clang-format</code> from the nearest parent directory)</p> <p>Common values: <code>LLVM</code>, <code>Google</code>, <code>Chromium</code>, <code>Mozilla</code>, <code>WebKit</code>, <code>Microsoft</code>, <code>file</code>.</p> <pre><code>clang-format-inc --style Google\n</code></pre>"},{"location":"cli/#-fallback-style-style","title":"<code>--fallback-style STYLE</code>","text":"<p>Style to use when <code>--style=file</code> is active but no <code>.clang-format</code> file is found.</p> <p>Default: none (clang-format uses <code>LLVM</code> as its own fallback)</p> <pre><code>clang-format-inc --fallback-style LLVM\n</code></pre>"},{"location":"cli/#-sort-includes","title":"<code>--sort-includes</code>","text":"<p>Pass <code>--sort-includes</code> to <code>clang-format</code>, enabling include sorting.</p> <p>Default: off</p>"},{"location":"cli/#-p-num","title":"<code>-p NUM</code>","text":"<p>Strip <code>NUM</code> leading path components from filenames in the diff. Matches the <code>-p</code> flag of <code>patch</code>.</p> <p>Default: <code>1</code> (strips git's <code>a/</code> / <code>b/</code> prefixes)</p>"},{"location":"cli/#-include-regex","title":"<code>--include REGEX</code>","text":"<p>Only process files whose path matches this regular expression.</p> <pre><code>clang-format-inc --include '\\.cpp$'\n</code></pre>"},{"location":"cli/#-exclude-regex","title":"<code>--exclude REGEX</code>","text":"<p>Skip files whose path matches this regular expression.</p> <pre><code>clang-format-inc --exclude 'third_party/'\n</code></pre>"},{"location":"cli/#-workers-n","title":"<code>--workers N</code>","text":"<p>Number of parallel <code>clang-format</code> processes.</p> <p>Default: <code>1</code> (sequential)</p>"},{"location":"cli/#modes-mutually-exclusive","title":"Modes (mutually exclusive)","text":""},{"location":"cli/#-check","title":"<code>--check</code>","text":"<p>Do not modify files. Exit non-zero and print a message if any changed line would be reformatted.</p>"},{"location":"cli/#-diff","title":"<code>--diff</code>","text":"<p>Do not modify files. Print a unified diff of what would change and exit non-zero if any file would be reformatted.</p>"},{"location":"cli/#environment-variables","title":"Environment variables","text":"<p>These are set automatically by pre-commit when running with <code>--from-ref</code>/<code>--to-ref</code>. You can also set them manually.</p> Variable Effect <code>PRE_COMMIT_FROM_REF</code> Base commit SHA for CI diff <code>PRE_COMMIT_TO_REF</code> Head commit SHA for CI diff <p>When both are set, <code>clang-format-inc</code> uses <code>git diff FROM TO</code>. Otherwise it uses <code>git diff --cached</code>.</p>"},{"location":"cli/#exit-codes","title":"Exit codes","text":"Code Meaning <code>0</code> Success \u2014 no changes needed (or changes applied) <code>1</code> Binary not found, git error, or (in check/diff mode) file would be reformatted other <code>clang-format</code> returned a non-zero exit code"},{"location":"how-it-works/","title":"How it works","text":"<p><code>clang-format-inc</code> is a thin orchestrator \u2014 it connects three standard tools (<code>git</code>, <code>clang-format</code>, and Python's <code>difflib</code>) to produce incremental formatting.</p>"},{"location":"how-it-works/#the-pipeline","title":"The pipeline","text":"<pre><code>1. Determine diff source\n      \u251c\u2500\u2500 CI:    git diff -U0 FROM_REF TO_REF -- &lt;files&gt;\n      \u2514\u2500\u2500 Local: git diff -U0 --cached -- &lt;files&gt;\n\n2. Parse the unified diff\n      \u2514\u2500\u2500 extract { filename \u2192 [(start, end), ...] }\n              (added-line ranges only; deletions and unchanged lines ignored)\n\n3. For each file, invoke clang-format on its ranges only\n      \u2514\u2500\u2500 clang-format --lines=start:end [--lines=...] --style=... -i &lt;file&gt;\n</code></pre>"},{"location":"how-it-works/#step-1-determining-the-diff-source","title":"Step 1 \u2014 Determining the diff source","text":"<p><code>main.py</code> checks the environment:</p> <ul> <li>CI mode: <code>PRE_COMMIT_FROM_REF</code> and <code>PRE_COMMIT_TO_REF</code> are both set \u2192 <code>git diff -U0 FROM TO</code></li> <li>Local mode: env vars absent (or only one is set) \u2192 <code>git diff -U0 --cached</code></li> </ul> <p>Using <code>-U0</code> (zero context lines) ensures the diff contains only the exact lines that changed, with no surrounding context to confuse the range parser.</p>"},{"location":"how-it-works/#step-2-parsing-the-diff","title":"Step 2 \u2014 Parsing the diff","text":"<p><code>diff_parser.py</code> is a pure function with no I/O. It reads the unified diff string and walks it line by line:</p> <ul> <li><code>+++ b/src/foo.cpp</code> \u2192 sets the current filename (stripping the <code>b/</code> prefix via <code>-p1</code>)</li> <li><code>@@ -old +new,count @@</code> \u2192 records <code>(new, new + count - 1)</code> as an added-line range</li> <li>Skips deletion-only hunks (<code>+N,0</code>) and deleted files (<code>+++ /dev/null</code>)</li> </ul> <p>The result is a dict: <code>{\"src/foo.cpp\": [(5, 8), (20, 22)], ...}</code>.</p>"},{"location":"how-it-works/#step-3-formatting-the-ranges","title":"Step 3 \u2014 Formatting the ranges","text":"<p><code>formatter.py</code> calls clang-format once per file with all its changed ranges:</p> <pre><code>clang-format --lines=5:8 --lines=20:22 --style=file -i src/foo.cpp\n</code></pre> <p>The <code>--lines</code> flag tells clang-format to reformat only those line ranges. Lines outside the ranges are output verbatim \u2014 the file structure is preserved, only the changed hunks get reformatted.</p>"},{"location":"how-it-works/#check-mode","title":"Check mode","text":"<p>In <code>--check</code> mode, <code>-i</code> is omitted. clang-format writes the formatted content to stdout. <code>formatter.py</code> compares stdout to the original file \u2014 if they differ, the file would have been changed and the hook returns non-zero.</p>"},{"location":"how-it-works/#diff-mode","title":"Diff mode","text":"<p>Same as check mode, but instead of just returning non-zero, <code>formatter.py</code> generates a unified diff using Python's <code>difflib.unified_diff</code> and writes it to stdout.</p>"},{"location":"how-it-works/#why-not-use-clang-format-diffpy","title":"Why not use <code>clang-format-diff.py</code>?","text":"<p>The LLVM project ships a script (<code>clang-format-diff.py</code>) that does something similar \u2014 it reads a unified diff and calls <code>clang-format --lines</code>. We deliberately don't use it:</p> <ol> <li>License \u2014 it's Apache-2.0; bundling it into an MIT package adds complexity</li> <li>Dependency maintenance \u2014 vendored files need tracking against upstream LLVM releases</li> <li>Scope \u2014 we only need the diff-parsing half, which is ~30 lines of Python</li> </ol> <p><code>clang_format_inc/diff_parser.py</code> reimplements that half cleanly, with full test coverage and no external dependency.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#as-a-pre-commit-hook-recommended","title":"As a pre-commit hook (recommended)","text":"<p>Add to your <code>.pre-commit-config.yaml</code>:</p> <pre><code>repos:\n  - repo: https://github.com/goyaladitya05/clang-format-inc\n    rev: v0.3.0\n    hooks:\n      - id: clang-format-inc\n</code></pre> <p>Then install the hooks:</p> <pre><code>pip install pre-commit\npre-commit install\n</code></pre> <p><code>clang-format</code> is automatically installed into the hook's isolated virtualenv via <code>additional_dependencies</code>. No system-wide <code>clang-format</code> install is needed.</p>"},{"location":"installation/#pin-a-specific-clang-format-version","title":"Pin a specific clang-format version","text":"<pre><code>hooks:\n  - id: clang-format-inc\n    additional_dependencies: ['clang-format==18.1.0']\n</code></pre> <p>Any version available on PyPI as the <code>clang-format</code> package can be pinned this way.</p>"},{"location":"installation/#as-a-standalone-cli-tool","title":"As a standalone CLI tool","text":"<pre><code>pip install clang-format-inc\n</code></pre> <p>You will also need <code>clang-format</code> available on your <code>PATH</code>, or pass <code>--binary</code> to specify its location:</p> <pre><code>clang-format-inc --binary /usr/lib/llvm-18/bin/clang-format foo.cpp\n</code></pre>"},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.9+</li> <li>Git (must be on <code>PATH</code>)</li> <li><code>clang-format</code> \u2014 installed automatically by pre-commit, or manually for standalone use</li> </ul>"},{"location":"usage/check-diff/","title":"Check &amp; diff modes","text":"<p>Both modes are read-only \u2014 they never modify files. They are mutually exclusive.</p>"},{"location":"usage/check-diff/#-check","title":"<code>--check</code>","text":"<p>Exits non-zero if any changed line would be reformatted. Prints a message identifying the file.</p> <pre><code>clang-format-inc --check foo.cpp\n# clang-format-inc: foo.cpp would be reformatted\n# exit code: 1\n</code></pre> <p>Use case: enforce formatting in CI without auto-fixing. Developers must fix formatting themselves before the PR can merge.</p> <pre><code>hooks:\n  - id: clang-format-inc\n    args: [--check]\n</code></pre>"},{"location":"usage/check-diff/#-diff","title":"<code>--diff</code>","text":"<p>Exits non-zero if any changed line would be reformatted, and prints a unified diff of exactly what would change.</p> <pre><code>clang-format-inc --diff foo.cpp\n</code></pre> <pre><code>--- a/foo.cpp\n+++ b/foo.cpp\n@@ -1 +1 @@\n-int x=1;\n+int x = 1;\n</code></pre> <p>Use cases:</p> <ul> <li>Debugging \u2014 see exactly what clang-format wants to change</li> <li>PR comment bots \u2014 parse the diff and post inline comments</li> <li>Pre-flight check before auto-formatting</li> </ul> <pre><code>hooks:\n  - id: clang-format-inc\n    args: [--diff]\n</code></pre>"},{"location":"usage/check-diff/#comparison","title":"Comparison","text":"Default <code>--check</code> <code>--diff</code> Modifies files \u2705 \u274c \u274c Exit non-zero on changes \u274c \u2705 \u2705 Shows what would change \u274c message only full unified diff"},{"location":"usage/ci/","title":"CI mode","text":"<p>When pre-commit is invoked with <code>--from-ref</code> and <code>--to-ref</code>, it sets <code>PRE_COMMIT_FROM_REF</code> and <code>PRE_COMMIT_TO_REF</code> in the environment. <code>clang-format-inc</code> detects these and diffs between the two refs instead of using the staging area.</p> <p>This solves the classic CI problem: the staging area is empty when pre-commit runs in CI, so <code>git diff --cached</code> would always return nothing.</p>"},{"location":"usage/ci/#github-actions","title":"GitHub Actions","text":""},{"location":"usage/ci/#using-the-pre-commit-action-simplest","title":"Using the pre-commit action (simplest)","text":"<pre><code># .github/workflows/pre-commit.yml\nname: pre-commit\n\non:\n  pull_request:\n\njobs:\n  pre-commit:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0  # needed so git can access the base ref\n\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - uses: pre-commit/action@v3.0.1\n        with:\n          extra_args: &gt;-\n            --from-ref ${{ github.event.pull_request.base.sha }}\n            --to-ref   ${{ github.event.pull_request.head.sha }}\n</code></pre>"},{"location":"usage/ci/#manual-invocation","title":"Manual invocation","text":"<pre><code>- name: Run clang-format-inc\n  run: |\n    pip install pre-commit\n    pre-commit run clang-format-inc \\\n      --from-ref ${{ github.event.pull_request.base.sha }} \\\n      --to-ref   ${{ github.event.pull_request.head.sha }}\n</code></pre>"},{"location":"usage/ci/#how-the-env-vars-are-used","title":"How the env vars are used","text":"<pre><code>PRE_COMMIT_FROM_REF=abc123   (base commit SHA)\nPRE_COMMIT_TO_REF=def456     (head commit SHA)\n  \u2514\u2500 git diff -U0 --no-color abc123 def456 -- &lt;files&gt;\n      \u2514\u2500 parse changed lines\n          \u2514\u2500 clang-format --lines=N:M -i &lt;file&gt;\n</code></pre>"},{"location":"usage/ci/#partial-env-var-guard","title":"Partial env var guard","text":"<p>If only one of <code>PRE_COMMIT_FROM_REF</code> / <code>PRE_COMMIT_TO_REF</code> is set (misconfiguration), <code>clang-format-inc</code> falls back to local mode rather than crashing.</p>"},{"location":"usage/ci/#check-only-in-ci","title":"Check-only in CI","text":"<p>If you want CI to report formatting issues without auto-applying fixes (common in open-source workflows), use <code>--check</code>:</p> <pre><code>hooks:\n  - id: clang-format-inc\n    args: [--check]\n</code></pre> <p>The hook will exit non-zero and print which files would be reformatted, without modifying them.</p>"},{"location":"usage/filtering/","title":"File filtering","text":"<p>By default, <code>clang-format-inc</code> processes every file that appears in the diff. Two flags let you narrow that down by matching file paths against regular expressions.</p>"},{"location":"usage/filtering/#-include-regex","title":"<code>--include REGEX</code>","text":"<p>Only process files whose path matches the regex. Files that don't match are silently skipped.</p> <pre><code># Only format .cpp files, skip headers\nclang-format-inc --include '\\.cpp$'\n</code></pre> <pre><code>hooks:\n  - id: clang-format-inc\n    args: ['--include', '\\.cpp$']\n</code></pre>"},{"location":"usage/filtering/#-exclude-regex","title":"<code>--exclude REGEX</code>","text":"<p>Skip files whose path matches the regex.</p> <pre><code># Skip anything under third_party/ or generated/\nclang-format-inc --exclude 'third_party/|generated/'\n</code></pre> <pre><code>hooks:\n  - id: clang-format-inc\n    args: ['--exclude', 'third_party/|generated/']\n</code></pre>"},{"location":"usage/filtering/#using-both-together","title":"Using both together","text":"<p><code>--include</code> is applied first, then <code>--exclude</code>. A file must pass both filters to be processed.</p> <pre><code>hooks:\n  - id: clang-format-inc\n    args:\n      - '--include'\n      - 'src/'       # only files under src/\n      - '--exclude'\n      - 'src/gen/'   # but not generated files inside src/\n</code></pre>"},{"location":"usage/filtering/#note-on-pre-commits-types_or","title":"Note on pre-commit's <code>types_or</code>","text":"<p>When used as a pre-commit hook, pre-commit already filters files by type (<code>c</code>, <code>c++</code>, <code>cuda</code>, <code>objective-c</code>) before passing them to <code>clang-format-inc</code>. The <code>--include</code>/<code>--exclude</code> flags are most useful when running <code>clang-format-inc</code> standalone from the CLI, or when you need finer-grained control than type detection provides.</p>"},{"location":"usage/local/","title":"Local mode","text":"<p>When no CI environment variables are set, <code>clang-format-inc</code> operates in local mode: it diffs the staging area against <code>HEAD</code> using <code>git diff --cached</code>.</p>"},{"location":"usage/local/#how-it-works","title":"How it works","text":"<pre><code>git commit\n  \u2514\u2500 pre-commit runs clang-format-inc\n      \u2514\u2500 git diff -U0 --cached -- &lt;staged files&gt;\n          \u2514\u2500 parse added-line ranges per file\n              \u2514\u2500 clang-format --lines=N:M -i &lt;file&gt;\n</code></pre> <p>Only lines that exist in the staged diff are formatted. Unstaged changes are never touched.</p>"},{"location":"usage/local/#example","title":"Example","text":"<pre><code># Commit well-formatted code alongside a bad new line\ngit add good_file.cpp          # already formatted \u2014 safe\ngit add new_feature.cpp        # badly formatted new code\ngit commit -m \"add feature\"\n# \u2192 only the changed lines in new_feature.cpp are formatted\n# \u2192 good_file.cpp is untouched\n</code></pre>"},{"location":"usage/local/#unstaged-changes","title":"Unstaged changes","text":"<p>If you modify a file but don't stage it, the hook will not touch it \u2014 even if those lines are badly formatted. Only staged changes are in scope.</p>"},{"location":"usage/local/#untracked-files","title":"Untracked files","text":"<p>New files that have never been committed have no prior revision to diff against. They are formatted in their entirety rather than incrementally. Stage the file with <code>git add</code> and the hook picks up all lines as \"added\".</p>"},{"location":"usage/parallel/","title":"Parallel processing","text":"<p>By default <code>clang-format-inc</code> processes one file at a time. For large CI jobs with many changed files, the <code>--workers</code> flag enables parallel execution.</p>"},{"location":"usage/parallel/#-workers-n","title":"<code>--workers N</code>","text":"<pre><code>clang-format-inc --workers 4 file1.cpp file2.cpp file3.cpp file4.cpp\n</code></pre> <pre><code>hooks:\n  - id: clang-format-inc\n    args: ['--workers', '4']\n</code></pre> <p>Each worker runs an independent <code>clang-format</code> process. The number of workers is capped by your machine's CPU count in practice \u2014 setting it higher than that provides no benefit.</p>"},{"location":"usage/parallel/#sequential-vs-parallel-behaviour","title":"Sequential vs parallel behaviour","text":"Sequential (default) Parallel (<code>--workers N</code>) Stops on first error \u2705 Yes \u274c No \u2014 all files run Output order Deterministic Deterministic (submission order) Diff output ordering In order In order <p>In parallel mode, all files are submitted at once and results are collected in the original file order, so diff output is always consistent regardless of which worker finishes first.</p>"},{"location":"usage/parallel/#when-to-use-it","title":"When to use it","text":"<p>For most projects, the default sequential mode is fast enough \u2014 pre-commit only passes changed files, and <code>clang-format</code> is very quick per file. Consider <code>--workers</code> when:</p> <ul> <li>Your CI job routinely sees tens or hundreds of changed files</li> <li>You're running <code>--all-files</code> on initial adoption across a large codebase</li> </ul>"},{"location":"usage/quickstart/","title":"Quick start","text":""},{"location":"usage/quickstart/#1-add-a-clang-format-file","title":"1. Add a <code>.clang-format</code> file","text":"<p>Put a style config in your repo root so clang-format knows your project's style:</p> <pre><code># .clang-format\nBasedOnStyle: LLVM\nIndentWidth: 4\nColumnLimit: 120\n</code></pre> <p>Common bases: <code>LLVM</code>, <code>Google</code>, <code>Chromium</code>, <code>Mozilla</code>, <code>WebKit</code>, <code>Microsoft</code>.</p> <p>Without this file, clang-format falls back to <code>LLVM</code> style.</p>"},{"location":"usage/quickstart/#2-add-the-hook","title":"2. Add the hook","text":"<pre><code># .pre-commit-config.yaml\nrepos:\n  - repo: https://github.com/goyaladitya05/clang-format-inc\n    rev: v0.3.0\n    hooks:\n      - id: clang-format-inc\n</code></pre>"},{"location":"usage/quickstart/#3-install","title":"3. Install","text":"<pre><code>pip install pre-commit\npre-commit install\n</code></pre>"},{"location":"usage/quickstart/#4-try-it","title":"4. Try it","text":"<p>Stage a badly-formatted C++ file and commit:</p> <pre><code>echo 'int main(){int x=1;return x;}' &gt; foo.cpp\ngit add foo.cpp\ngit commit -m \"test\"\n</code></pre> <p>The hook runs, formats only the changed lines in-place, and pre-commit detects the modification \u2014 the commit is blocked and you see:</p> <pre><code>clang-format-inc...........................................................Failed\n- hook id: clang-format-inc\n- files were modified by this hook\n</code></pre> <p>Run <code>git add foo.cpp</code> and commit again \u2014 this time it passes because the file is now formatted.</p>"},{"location":"usage/quickstart/#5-run-manually","title":"5. Run manually","text":"<pre><code># Format staged changes right now\npre-commit run clang-format-inc\n\n# Format all files (useful for initial setup)\npre-commit run clang-format-inc --all-files\n</code></pre>"}]}